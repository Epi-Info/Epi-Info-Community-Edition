namespace Epi.Windows.Enums
{
    public enum TrackerStatus
    {
        NotSelected,
        Selected,
        Resize
    }

    public enum Print
    {
        BlankForm,
        CurrentRecord,
        AllRecords
    }
}
